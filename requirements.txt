# YouTube Shorts AI Agent — Phase 1 Dependencies
# All free / open-source. No paid APIs required.
# Install: pip install -r requirements.txt

# Core
requests>=2.31.0

# Voice generation (free, local via Microsoft Edge TTS)
edge-tts>=7.0.0

# Video assembly (background generation, file handling)
Pillow>=10.0.0

# Article extraction (Step 1.5 — fetch real article text instead of the
# ~400-char RSS blurb). trafilatura is primary, readability-lxml the fallback;
# article_fetcher also has a stdlib HTML-stripper fallback so a missing dep
# never hard-fails the pipeline, but quality is best with both installed.
trafilatura>=2.0.0
readability-lxml>=0.8.0

# YouTube transcripts (Step 1.5 — YouTube-source stories fetch captions via
# youtube-transcript-api instead of scraping the watch page, whose extraction
# always failed). On 1.x: 0.6.2 stopped working against current YouTube
# (empty timedtext responses → xml ParseError "no element found"); the 1.x
# rewrite tracks YouTube's changes, and article_fetcher uses its instance
# .fetch() API.
youtube-transcript-api==1.2.2

# Assembly also requires ffmpeg (install via apt/brew/choco):
#   sudo apt install ffmpeg        # Linux
#   brew install ffmpeg            # macOS
#   winget install ffmpeg          # Windows

# --- Optional Dependencies ---
# For stock footage: No extra deps needed (uses stdlib urllib)
# Get a free Pexels API key at: https://www.pexels.com/api/
# Get a free Pixabay API key at: https://pixabay.com/api/docs/
